{
  "name": "codomon",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "act": "act -j playwright --secret-file .secrets"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.1"
  }
}
